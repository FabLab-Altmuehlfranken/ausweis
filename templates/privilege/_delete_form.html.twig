<form method="post" action="{{ path('app_privilege_delete', {'id': privilege.id}) }}" onsubmit="return confirm('Möchtest du die Berechtigung\n\'{{ privilege.displayName }}\'\nwirklich löschen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_privilege' ~ privilege.id) }}">
    <button class="action">🗑 Löschen</button>
</form>

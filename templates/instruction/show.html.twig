{% extends 'base.html.twig' %}

{% block title %}Einweisung{% endblock %}

{% block body %}
    <h1>Instruction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ instruction.name }}</td>
            </tr>
            <tr>
                <th>Berechtigungen</th>
                <td>
                    <ul>
                        {% for privilege in instruction.privileges %}
                            <li>
                                <a href="{{ path('app_privilege_show', {'id': privilege.id}) }}">
                                    {{ privilege.name }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_instruction_edit', {'id': instruction.id}) }}">✏️ Bearbeiten</a>

    {{ include('instruction/_delete_form.html.twig') }}
{% endblock %}

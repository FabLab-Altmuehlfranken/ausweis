{% extends 'base.html.twig' %}

{% block title %}Bereich{% endblock %}

{% block body %}
    <h1>Bereich</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ area.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_area_edit', {'id': area.id}) }}">bearbeiten</a>

    {{ include('area/_delete_form.html.twig') }}
{% endblock %}
